"use client";

import React from "react";
import { PublisherPage as PublisherView } from "@/views/PublisherPage";

export default function PublisherPage() {
  return <PublisherView onNavigate={() => {}} categories={[]} />;
}
