"use client";

import React from "react";
import { TermsPage as TermsView } from "@/views/TermsPage";

export default function TermsPage() {
  return <TermsView onNavigate={() => {}} categories={[]} />;
}
