"use client";

import React from "react";
import { AboutPage as AboutView } from "@/views/AboutPage";

export default function AboutPage() {
  return <AboutView onNavigate={() => {}} categories={[]} />;
}
