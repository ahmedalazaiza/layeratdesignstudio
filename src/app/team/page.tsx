"use client";

import React from "react";
import { TeamPage as TeamView } from "@/views/TeamPage";

export default function TeamPage() {
  return <TeamView onNavigate={() => {}} categories={[]} />;
}
